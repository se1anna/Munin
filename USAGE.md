# Munin 博客系统使用指南

本文档提供 Munin 博客系统的完整功能说明与使用示例，包含后台操作、内容撰写、主题切换、OAuth 统一身份认证接入、HOTP 自动化发文接口以及安全防护配置。

---

## 目录

1. [管理后台登录与账户安全](#1-管理后台登录与账户安全)
2. [文章创作与编辑器使用](#2-文章创作与编辑器使用)
3. [视觉主题管理与全站缓存重建](#3-视觉主题管理与全站缓存重建)
4. [OAuth 统一身份认证与单点登录接入](#4-oauth-统一身份认证与单点登录接入)
5. [一次性密钥池（HOTP）与自动化发文](#5-一次性密钥池hotp与自动化发文)
6. [媒体文件管理与 R2 分发](#6-媒体文件管理与-r2-分发)
7. [全站数据备份与导出](#7-全站数据备份与导出)
8. [邮件通知服务配置](#8-邮件通知服务配置)
9. [安全攻防与防账单消耗机制](#9-安全攻防与防账单消耗机制)

---

## 1. 管理后台登录与账户安全

### 1.1 登录管理后台
- 后台访问路径：`/admin` 或 `/login`。
- 本地初始管理员账户：
  - 用户名：`admin`
  - 密码：`admin123456`
- 登录成功后，系统会为当前会话签发经由 `JWT_SECRET` 加密签名的认证凭证，并存入 Cookie。

### 1.2 修改管理员密码与跨设备失效联动
- 在后台侧边栏点击**管理员账户**。
- 输入旧密码及两次新密码后确认修改。
- **密码强度要求**：新密码长度须在 8 位以上，且必须同时包含大写字母、小写字母、数字及特殊字符（如 `!@#$%` 等）。
- **安全机制**：密码修改成功后，数据库中的令牌版本号将自动递增，此前在所有其他设备或浏览器上登录的历史会话及关联的 OAuth 访问令牌均会**即刻失效**，防止旧凭证被盗用。

---

## 2. 文章创作与编辑器使用

编辑器支持三种工作模式，可随时无缝切换：

### 2.1 可视化区块排版模式（推荐）
可视化模式采用区块化排版结构，支持以下区块类型：
- **段落区块**：输入正文内容，支持在文本中混合使用 Markdown 语法（如粗体 `**文字**`、斜体 `*文字*`、链接 `[描述](url)`）。
- **标题区块**：支持二级标题（H2）、三级标题（H3）、四级标题（H4）级别切换。
- **图像区块**：输入图片链接与可选图注文字，即时展示图片缩略预览。
- **代码区块**：提供语言标识输入框（如 `ts`、`rust`、`python`）并采用等宽字体排版。
- **引用区块**：支持输入引言正文与作者/出处署名。
- **列表区块**：支持一键切换无序要点列表与有序编号列表，可动态增删列表条目。
- **分割线**：插入章节分界横线。
- **自定义 HTML**：支持插入自定义 HTML 结构或嵌入代码（为防止存储型 XSS，Markdown 渲染器会过滤原始 HTML 标签；页脚自定义 HTML 会剥离 `<script>`、`<iframe>` 及事件处理器等危险内容）。

**区块操作**：每个区块卡片右上角均提供**上移**、**下移**、**下方插入**及**删除**操作按钮。

### 2.2 源码与 Markdown 模式
- 点击顶部**源码与 Markdown**标签进入纯文本编辑界面。
- 支持直接输入或粘贴标准的 Markdown 文本。
- 粘贴完成后，点击右上角**将 Markdown 转换为可视化区块**按钮，系统将自动将 Markdown 语法解析并转换为结构化的区块卡片。

### 2.3 实时排版预览模式
- 点击顶部**实时排版预览**标签，系统将调用内置渲染引擎实时输出最终读者在前台看到的排版样式。

### 2.4 文章高级设置
点击标题下方的**文章高级设置**展开面板，可配置：
- **自定义别名（Slug）**：如 `my-first-article`，前台访问链接将呈现为 `/post/my-first-article`；若留空则自动生成。
- **特色封面图链接**：设置文章列表展示的头图链接。
- **文章摘要**：自定义展示于列表页的摘要文字；若留空将自动截取正文前 160 字。

---

## 3. 视觉主题管理与全站缓存重建

系统内置主题自识别机制，所有主题存放于 `src/themes/` 目录下。

### 3.1 对比预览与切换
1. 进入后台**系统设置**页签。
2. 在**视觉主题管理与全站缓存重建**模块中，通过下拉菜单选择目标主题。
3. 界面会以**左右双卡片**形式并列展示：
   - **左侧**：当前线上生效的主题名称、调色板、主要字体及设计亮点。
   - **右侧**：所选目标主题的对应效果预览。
4. 点击**确认更换并清空缓存重建**按钮，服务端将在后台完成：
   - 更新数据库中的当前激活主题；
   - 扫描并批量清除 Cloudflare KV 上的全量页面缓存；
   - 触发首页服务端渲染预热。

### 3.2 自定义主题开发
在 `src/themes/` 目录下新建一个以主题 ID 命名的子目录（例如 `src/themes/custom-theme/`），并提供以下 4 个文件即可被系统自动识别：
- `meta.ts`：导出主题元数据对象（名称、版本、作者、调色板预览等）。
- `theme.json`：WordPress 格式的主题调色板与布局尺寸变量配置。
- `styles.ts`：主题专有的全局 CSS 样式代码。
- `templates.ts`：导出首页列表模板与文章详情页模板函数。

---

## 4. OAuth 统一身份认证与单点登录接入

博客系统内置了 OAuth 与 OpenID Connect 兼容的统一身份认证服务，可将博客作为账号中心为其他外部应用提供单点登录。

### 4.1 核心端点地址
- **发现配置地址**：`https://your-blog.com/.well-known/openid-configuration`
- **授权端点**：`https://your-blog.com/oauth/authorize`
- **令牌端点**：`https://your-blog.com/oauth/token`
- **用户信息端点**：`https://your-blog.com/oauth/userinfo`

### 4.2 注册客户端应用
1. 进入后台 **OAuth 应用** 页签，点击 **+ 创建应用**。
2. 填写应用信息：
   - **应用名称**：例如 `知识库系统`。
   - **回调地址**：一行一个，例如 `https://kb.example.com/api/auth/callback`（服务端严格进行白名单前缀匹配，拦截未授权域名的重定向攻击）。
   - **权限范围**：默认为 `openid profile email role`。
   - **受信任属性**：若勾选为受信任应用，当用户已在博客登录时将触发无感重定向登录，无需每次手动点击确认授权。
3. 点击创建后，页面顶部将展示生成的 **Client Secret**（仅显示一次，请妥善保存）。

### 4.3 客户端接入调用流程（以 Node.js / cURL 为例）

#### 第一步：引导用户跳转至授权端点
外部系统将用户浏览器重定向至博客授权页面：
```
https://your-blog.com/oauth/authorize?response_type=code&client_id=CLIENT_ID&redirect_uri=https://kb.example.com/api/auth/callback&scope=openid%20profile%20email&state=RANDOM_STATE
```

#### 第二步：回调处理并换取访问令牌
用户授权后，博客将重定向回回调地址并附带 `code` 参数：
```
https://kb.example.com/api/auth/callback?code=AUTH_CODE_HERE&state=RANDOM_STATE
```
外部系统后端向博客令牌端点发起 POST 请求：
```bash
curl -X POST https://your-blog.com/oauth/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=authorization_code" \
  -d "code=AUTH_CODE_HERE" \
  -d "client_id=CLIENT_ID" \
  -d "client_secret=CLIENT_SECRET" \
  -d "redirect_uri=https://kb.example.com/api/auth/callback"
```

响应示例：
```json
{
  "access_token": "eyJhbGciOi...",
  "token_type": "Bearer",
  "expires_in": 86400,
  "scope": "openid profile email role",
  "id_token": "eyJhbGciOi..."
}
```

#### 第三步：读取用户信息
携带 `access_token` 请求用户信息端点：
```bash
curl -X GET https://your-blog.com/oauth/userinfo \
  -H "Authorization: Bearer ACCESS_TOKEN_HERE"
```

响应示例：
```json
{
  "sub": "user_id_string",
  "name": "管理员",
  "email": "admin@example.com",
  "role": "administrator"
}
```

---

## 5. 一次性密钥池（HOTP）与自动化发文

HOTP 是一套为脚本、自动化工具、CI/CD 流程设计的安全认证机制。通过单次消耗、即时核销的 Token 避免在脚本中保存长期有效的管理员静态密码。

### 5.1 生成并下载密钥池
1. 进入后台 **HOTP 鉴权** 页签。
2. 点击 **生成 HOTP 密钥池并下载** 按钮。
3. 服务端将在后台分批并发写入 Cloudflare KV，并立即触发 `hotp-keys.txt` 文件下载（包含 1000 个随机一次性 Token）。
4. **频率保护**：生成接口设有 10 分钟冷却锁，防止重复触发写满 KV 配额。

### 5.2 使用一次性密钥自动发布文章
在调用专用 API 时，在请求头中携带该一次性 Token（请求头键名由 `HOTP_HEADER_KEY` 定义，默认为 `X-HOTP-Key`）。

**cURL 调用示例**：
```bash
curl -X POST https://your-blog.com/api/hotp/publish \
  -H "X-HOTP-Key: a1b2c3d4e5" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "自动发布的文章标题",
    "content": "## 章节标题\n\n通过脚本自动发布的内容...",
    "status": "published",
    "slug": "auto-published-article"
  }'
```

*(也支持标准请求头格式：`-H "Authorization: HOTP a1b2c3d4e5"`)*

**核销特性**：服务端校验成功后，会**即刻从 KV 中删除该 Token**。即使请求被抓包，该 Token 也无法被二次使用。

---

## 6. 媒体文件管理与 R2 分发

博客系统集成 Cloudflare R2 对象存储，用于存放文章配图与多媒体文件。

### 6.1 上传媒体文件
- 在后台 **媒体库** 页签，点击 **上传新媒体文件** 按钮选择本地文件上传；
- 或在文章编辑器中直接通过图片区块引用。

### 6.2 安全校验机制
- **文件头魔数校验**：服务端会读取文件二进制头部魔数（Magic Number），精准识别文件真实类型，杜绝后缀伪装（例如伪装成 `.jpg` 的恶意脚本）。
- **SVG 内容过滤**：若上传 SVG 文件，服务端会自动扫描并过滤 `<script>`、`onload=` 等潜在 XSS 攻击向量。
- **单文件大小限制**：默认上限为 10MB。

### 6.3 CDN 访问路径
上传成功后，可通过 `/media/{r2_key}` 路径直接在全网访问该静态资源，享受边缘 CDN 缓存加速。

---

## 7. 全站数据备份与导出

### 7.1 一键数据备份
- 进入后台 **数据备份** 页签。
- 点击 **下载数据备份** 按钮，系统将实时从 Durable Objects SQLite 数据库中读取全量数据，并生成标准的 JSON 文件流下载到本地。

### 7.2 备份内容包含
- 用户列表与角色数据
- 文章、草稿与页面内容
- 分类与标签体系
- 全量评论记录及审核状态
- 媒体附件元数据
- 站点全局配置与 OAuth 客户端应用信息

---

## 8. 邮件通知服务配置

系统采用 Cloudflare 原生 **Email Sending Worker 绑定**，无需注册任何第三方邮件发送平台。

### 8.1 发信域名设置
在 `wrangler.jsonc` 中配置您的发件人邮箱：
```jsonc
{
  "send_email": [
    {
      "name": "EMAIL"
    }
  ],
  "vars": {
    "ADMIN_EMAIL": "welcome@your-domain.com"
  }
}
```
*注：`ADMIN_EMAIL` 必须为已经在 Cloudflare 控制台完成域名解析与验证的域名。*

### 8.2 触发场景
1. **身份验证码**：登录或重置凭证时自动发送 6 位数字验证码（有效期 5 分钟）。
2. **新评论与回复提醒**：读者发表评论或收到回复时自动发送格式化邮件通知。

---

## 9. 安全攻防与防账单消耗机制

为应对公开网络环境中的爬虫遍历、DDoS 及恶意刷量，系统构建了多层边缘防护机制：

| 防御维度 | 机制说明 | 保护效果 |
| :--- | :--- | :--- |
| **404 负向缓存** | 当恶意爬虫大量遍历不存在的文章 Slug 时，首次查询后在边缘 KV 中写入 404 负向缓存（TTL 60 秒） | 后续重复恶意请求直接在边缘被拦截并返回 404，不唤醒 Durable Objects，杜绝计费倒转 |
| **防撞库暴力破解** | 同一用户名或 IP 连续登录失败 5 次，自动触发 15 分钟临时锁定 | 拦截字典撞库攻击 |
| **密码强度强制** | 注册与修改密码要求 8 位以上，且必须包含大小写字母、数字及特殊字符 | 杜绝弱口令被暴力破解 |
| **密码哈希加固** | 采用 PBKDF2-SHA256 算法，600,000 次迭代（OWASP 2023 推荐值），随机盐值 16 字节 | 即使数据库泄露，攻击者难以离线破解密码 |
| **JWT 令牌失效联动** | 密码修改后自动递增令牌版本号，所有历史会话及 OAuth 访问令牌即刻失效 | 防止密码修改后旧凭证被盗用 |
| **邮件防轰炸** | 验证码发送设置 60 秒冷却；单篇文章评论通知设置 5 分钟冷却 | 防止邮件发送额度被恶意耗尽 |
| **HOTP 伪造拦截** | 前置正则严格校验 10 位小写字母+数字格式 | 格式不合法的请求直接在内存层拦截，不产生任何 KV 查询开销 |
| **HOTP 生成冷却** | 密钥池生成接口限制 10 分钟触发一次 | 防止高频并发生成写满 KV 配额 |
| **Markdown XSS 防御** | Markdown 渲染器禁用原始 HTML 直通（`html: false`），所有 HTML 标签自动转义 | 即使文章内容包含 `<script>` 等恶意标签，也不会在读者浏览器中执行 |
| **评论 XSS 防御** | 评论作者昵称与评论正文在服务端渲染时全量进行 HTML 实体转义 | 防止通过评论区注入恶意脚本窃取管理员 Cookie |
| **页脚 HTML 过滤** | 自定义页脚 HTML 自动剥离 `<script>`、`<iframe>`、`<object>`、事件处理器及 `javascript:` 伪协议 | 防止管理员误操作或账户被盗后在页脚注入恶意代码 |
| **CSRF 防护** | 管理后台所有写操作（POST/PUT/DELETE）强制校验 Origin/Referer 头，拒绝跨站请求 | 防止攻击者通过第三方网站伪造管理员操作 |
| **Turnstile 人机验证** | 注册、登录、评论、密码重置等公开端点强制 Cloudflare Turnstile 验证 | 拦截自动化脚本批量注册、撞库、垃圾评论 |
| **媒体上传魔数校验** | 服务端读取文件二进制头部魔数精准识别真实类型，SVG 文件额外扫描危险标签 | 杜绝后缀伪装攻击（如 `.jpg` 实为恶意脚本） |
| **OAuth 回调校验** | redirect_uri 严格匹配白名单 origin、pathname 及 query string；启用 PKCE S256 防授权码拦截 | 阻止授权码重定向劫持攻击 |
| **SSO 登录防钓鱼** | 登录页 return_to 参数强制校验同源或相对路径，拒绝外部 URL | 防止通过伪造登录页收集用户凭据 |
